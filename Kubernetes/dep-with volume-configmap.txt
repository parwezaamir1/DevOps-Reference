apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-python-app
  labels:
    app: sample-python-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sample-python-app
  template:
    metadata:
      labels:
        app: sample-python-app
    spec:
      containers:
      - name: python-app
        image: aamirjohn777/python-sample-app-demo:v1
        volumeMounts:
          - name: db-connect
            mountPath: /opt
#        env:
#        - name: DB-PORT
#           valueFrom:
#             configMapKeyRef:
#              name: test-cm
#              key: db-port
        ports:
        - containerPort: 8000
      volumes:
        - name: db-connect
          configMap:
            name: test-cm


 ####### configmap.yml
 apiVersion: v1
kind: ConfigMap
metadata:
  name: test-cm
data:
  db-port: "3308"

###### service.yml

apiVersion: v1
kind: Service
metadata:
  name: python-django-sample-app
spec:
  type: NodePort
  selector:
    app: sample-python-app
  ports:
    - port: 80
      targetPort: 8000
      # Optional field
      nodePort: 30007


######### ingress.yml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-example
spec:
  rules:
  - host: "foo.bar.com"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: python-django-sample-app
            port:
              number: 80




                                                 